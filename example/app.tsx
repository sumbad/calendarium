import * as React from 'react';
import * as ReactDOM from 'react-dom';

import {Calendarium} from '../src/Calendarium';

ReactDOM.render(React.createElement(Calendarium), document.getElementById('app'));